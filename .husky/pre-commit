# Deaktivert midlertidig: Prettier reformaterer hele filer, noe som gir store
# diffser når filene ikke er pre-formatert. Aktiver igjen etter at teamet har
# kjørt `pnpm format` samlet på main.
# pnpm lint-staged
